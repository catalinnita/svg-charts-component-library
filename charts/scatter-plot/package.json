{
    "name": "@sd-charts/scatter-plot",
    "displayName": "ScatterPlot",
    "description": "The *ScatterPlot* is used for simple data series",
    "version": "0.0.1",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "license": "MIT",
    "repository": {
        "type": "git",
        "utl": "https://github.com/catalinnita/svg-charts-component-library"
    },
    "scripts": {
        "build": "tsc src/index.tsx --declaration --jsx react-jsx --outDir ./dist",
        "dev": "tsup src/index.tsx --format esm,cjs --watch --dts --external react",
        "lint": "TIMING=1 eslint src --fix",
        "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
    },
    "dependencies": {
        "react": "^17.0.2",
        "@sd/data": "^0.0.1",
        "@sd/colors": "^0.0.1"
    },
    "devDependencies": {
    }
}
